---
name: context-editor-ui
description: 使用统一规范构建对话, 上下文编辑与行内评审界面
user-invocable: true
---

# context-editor-ui

## 应用流程

1. 阅读 `DESIGN.md`, 再读取 `system/design-system.config.js` 的五级规范与状态规则
2. 修改配置中的变量与参数, 在对应组件文件实现结构和行为, 在 `system/styles.css` 实现视觉与响应式状态
3. 新控件先实现为 `system/components` 中的共享组件并提供独立规范页, 页面只组装组件, 保存会话数据和连接事件, 不复制组件变量, 样式或行为
4. 按 `README.md` 的接口与加载顺序集成, 核对 12 个组件规范页, `system/app.html` 和 `system/examples/workbench.html`

## 约束与验收

- 范围为对话, 上下文与关联评审, 支持连续阅读和并排文档, 不扩展为无关仪表盘
- 待处理内容先并入编辑草稿, 确认后更新发送快照, 未确认修改不进入消息
- 模型, 审批, 添加和会话菜单的所有选项使用 `--menu-item-radius: var(--radius-control)` 的 8px 圆角矩形, 多行选项保持相同圆角, 不继承浮窗大圆角或工具栏入口胶囊形状, 默认透明, 选中只有对勾, hover 使用统一灰底, 键盘焦点轮廓保留, 复选 hover 不放大, 输入聚焦保持浅色边线, 保存与取消区分主次
- 完全访问的标题, 图标和对勾使用权限红色, 描述中性, 上下文标识与分组标签使用琥珀色表达控件与分组结构, 复选与正文保持中性
- 规范章节与条目共用紧凑对齐列, 标题与说明间隔 16px, 卡片组后保留 24px, 备注浮窗用标签和间距区分内容, 圆角内容区在查看, 编辑与 hover 时始终与面板同色, 浅色白底, 深色读取对应面板底色, 只有具体按钮在 hover 时使用 `--action-hover-bg` 灰底, 内部不使用分割线
- 组件共享颜色, 字体, 圆角, 图标, 点击区和边距, 深浅主题保持相同交互, 独立文案动作使用 Button, 结束当前编辑或确认流程的提交, 取消和弹窗删除使用 soft 常显浅底, 设置, 切换, 查看及可选操作使用 ghost 默认透明, 所有按钮 hover 与按下统一使用 `--action-hover-bg` 中性灰底, 该变量读取 `--surface-hover`, 不按 tone 改变 hover 底色, 文字与图标保留语义颜色, 不因菜单展开保持底色
- Button 的 variant, shape, tone 分别控制背景, 圆角和语义, 默认 soft / rounded / neutral, 中性可选操作为灰字, 删除为红字, 需注意的选择为紫字, 确认保存为蓝字, 并入与全部并入统一为绿色, hover 保留字色, 无描边, 保留键盘 focus-visible 轮廓
- 常规面板按钮采用 rounded 的 10px 圆角, 圆形控件集中的工具栏采用 pill 的 9999px 胶囊圆角, Composer 审批与模型入口默认透明并使用胶囊形 hover 背景, 会话切换入口默认透明, 上下文计数与备注数量标签使用两侧平直的 10px 圆角矩形, 作为状态摘要保留常显浅灰背景, 背景高 30px
- 文案按钮视觉高 32px, 粗指针点击区 44px, hover 不改变尺寸, 分组不显示副标题, 上下文底栏只保留右对齐确认按钮
- 输入框最大 680px 居中, 评审面板最大 560px, 添加菜单最大 320px, 上下文分组标题 14px / 500, 窄屏按可用宽度收缩
- 点击区与视觉底板分别定义, 只有粗指针扩大点击区, 窄窗口不放大背景或行间距
- 使用 `Composer` 的受控输入和 `accessory` 插槽组合备注, `onBeforeSend` 同步返回 `false` 时保留草稿, 不改写输入组件的发送逻辑
- 使用 `Panel`, `SessionSwitcher`, `ReviewPanel` 和 `AnnotationPopover` 复用面板与评审交互, 多会话通过 `active` 和 `triggerRef` 隔离菜单与焦点
- 功能图标共享本地节点数据, Agent 从蓝, 紫, 琥珀, 珊瑚和青色五款分配, 同一身份的开始和完成状态保持一致
- 验证键盘操作, 菜单关闭, 编辑与备注, 上下文确认, 发送快照和窄屏布局
- 说明使用中文, 保留系统名称, 代码标识和模型原名, 记录实际完成的验证
