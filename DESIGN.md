# context-editor-ui

新增或修改组件与页面时, 先阅读 [设计系统使用指南](DESIGN_SYSTEM_GUIDE.md), 本文件维护已确认的设计约束和交互边界

## 范围与来源

面向对话, 上下文编辑与评审协作, 最新示例为 [行内工作台](system/examples/workbench.html), 保留 [连续对话](system/app.html) 用于验证原有组件组合

- `system/design-system.config.js`: 设计变量, 五级规范, 状态规则, 组件参数和示例数据的唯一来源
- `system/styles.css`: 组件视觉, 响应式布局和状态, 页面不添加组件样式覆盖
- `system/components`: 复用组件及行为, `system/examples/workbench.js` 只保存会话数据, 连接组件事件和组织页面布局
- `system/icons.data.js`: Lucide 1.17.0 子集及本地命令窗口图形, `icon-set.js` 和 `Icons.js` 使用同一份节点数据, 分别提供 DOM 和 React 接口
- `system/code-format.js`: 消息代码, 工具输出和 Markdown 原文共用的文本分词与 DOM/React 渲染, 复制保留原文
- `system/interactions.js`: 复制与状态反馈, `motion-runtime.js` 和 `MotionPresence.js` 负责过渡
- 基础规范页从配置渲染, 规范文字不得与实际控件状态冲突

## 五级规范

1. 基础值: 色板, 系统字体, 字级, 间距, 圆角, 图标尺寸, 动效
2. 语义角色: 正文, 辅助文字, 背景, 边线, 焦点, 主次操作与身份色
3. 控件: 按钮, 复选, 菜单, 编辑输入, 默认与交互状态
4. 组合组件: 消息, 输入框, 上下文编辑, 评审清单, 备注浮窗
5. 页面模板: 连续对话与并排评审, 窄屏按阅读顺序叠放

数值以共享配置为准, 示例直接组合系统组件, 不保留示例专用的组件变量, 样式副本或交互实现

## 视觉与控件

- 浅色画布使用白色, 正文深灰, 用户气泡浅蓝, 链接蓝色, 模型提示紫色, 文件和语法提示琥珀色
- 已完成, 复选, 评论及评审操作使用中性色, 不使用绿色表达完成
- 正文 13px, 操作文字 12px, 元信息 11px, 面板标题和上下文分组标题 14px/500, 辅助说明 12px/400
- 普通图标 16px, 行内编辑与评论 14px, 描边 1.5, 评论向上校准 1px, 箭头 11px
- Agent 使用 20px 透明彩色图形, 默认目录为蓝色, 紫色, 琥珀, 珊瑚和青色五款, 首次出现时分配图形, 同一身份在当前页面的开始和完成状态保持一致, 薄荷环形仅归档
- 视口宽度只控制布局, 点击区根据指针类型调整, 普通图标按钮在精细指针下为 28px, 在 `pointer: coarse` 下为 44px, 视觉底板保持 28px, 触屏输入文字使用 16px
- 复选底板为 18px 浅灰方块, 圆角 4px, 选中只增加对勾, hover 只换底色, 不扩大底板或高亮整行
- 模型, 审批, 添加和会话菜单的所有选项共用 `--menu-item-radius: var(--radius-control)`, 固定为 8px 圆角矩形, 含说明的多行选项也遵循此值, 不继承浮窗的大圆角或工具栏入口的胶囊形状, 选中仅显示右侧对勾, 默认透明, hover 使用统一灰底, 键盘按钮保留细灰焦点线
- 完全访问使用 `--permission-danger` 标识标题, 图标和选中对勾, 描述仍使用中性色, 红色不扩展到菜单背景
- 主输入, 备注输入, 行内编辑和新增内容共用浅色边线, 聚焦不加深边框, 不叠加黑框或光效
- 所有独立文案动作复用 `Button`, 按当前流程的操作职责选择背景: 结束当前编辑或确认流程所需的提交, 取消和弹窗删除等决策使用 `variant="soft"`, 常显语义浅底, 设置, 切换, 查看及可选操作使用 `variant="ghost"`, 默认透明, 所有按钮 hover 与按下统一使用 `--action-hover-bg` 中性灰底, 该变量读取 `--surface-hover`, 不按 tone 改变 hover 底色, 文字与图标保留语义颜色, 菜单展开本身不让入口背景常亮
- 语义与背景分别选择: neutral 为中性, soft 配正文色与浅灰底, ghost 使用灰字, confirm 为蓝字, merge 为绿色, 并入与全部并入使用同一颜色, danger 为红字, attention 为紫字, hover 不改变字色, 深色主题读取同一组语义变量, 按钮无描边和阴影, 键盘操作保留 `focus-visible` 轮廓
- 圆角与背景分别选择: 常规面板操作使用 `shape="rounded"` 的 10px 圆角矩形, 在圆形控件集中的工具栏使用 `shape="pill"` 的 9999px 胶囊圆角, Composer 的审批和模型入口采用透明默认背景与胶囊形 hover 背景, 会话切换入口默认透明
- 文案按钮视觉高 32px, 字号 13px, 行高 20px, 左右内边距 14px, 图文间隔 6px, 粗指针点击区 44px, 单行视觉背景仍为 32px, 普通图标底板圆角 8px, 独立面板圆角 20px, 面板内边距 16px
- 资料标签使用白底细边框和胶囊圆角, 上下内边距 6px, 左右 12px, 图标 14px, 图文间隔 6px, 标签间隔 8px, 图标颜色读取统一的类别色
- 工具图标按类别区分: 网页使用蓝色, 命令使用琥珀色, 通用工具使用紫色, 摘要文字和完成状态保持中性色
- 上下文标识和分组标签使用 `--context-accent` 琥珀色表达控件与分组结构, 条目正文与复选保持中性色, 命令图标使用 24 x 24 画布内的 20 x 14 窗口轮廓, 等比缩放图形与描边
- 代码块使用浅灰背景, 深色正文, 标题, 键, 字符串和关键字使用琥珀色, 浅色主题为 `#99670a`, 深色主题为 `#eac16b`, 列表标记使用蓝色, 不按完成状态改变文本颜色
- 阴影仅用于输入框, 独立评审和浮窗, 助手正文与工具行保持无框

## 控件几何

点击区, 图标和背景分别读取共享变量, hover 不改变任何一层的尺寸, 规范页入口为 [控件几何](system/foundations.html#control-geometry)

| 控件 | 视觉和间距 | 点击区与响应式 |
| --- | --- | --- |
| 文案动作 | 32px 视觉高度, rounded 为 10px 圆角, pill 为 9999px, 无描边, soft 常显浅底, ghost 默认透明 | 精细指针 32px, 粗指针 44px, hover 不改变尺寸 |
| 普通图标操作 | 28px 视觉底板, 8px 圆角, hover 使用灰底 | 精细指针 28px, 粗指针 44px, 不根据窗口宽度放大 |
| Composer 底部工具栏 | 44px 高, 图标 hover 背景 32px 圆形, 权限和模型入口默认透明, hover 背景高 32px, 两端为胶囊圆角 | 加号区域 44px, 麦克风和发送区域宽 36px, 粗指针下宽 44px |
| Composer 发送与间距 | 发送圆 30px, 右侧按钮间隔 2px, 右侧留白 5px | 发送圆到输入框外边缘保留 8px, 含 7px 内容留白与 1px 边框 |
| 已确认, 待处理与备注标签 | 两侧平直的 10px 圆角矩形, 常显浅灰背景固定 30px 高, 图文间隔 6px, 内容不换行 | 粗指针点击区可为 44px, 背景不随之增高 |
| 代码块 | 字号 12px, 行高 1.8, 内边距 16px, 圆角 20px | 代码允许换行, 保留原始文本和换行符 |

## 组合与内容边界

- `Button` 统一独立文案动作, `variant`, `shape`, `tone` 分别控制背景层级, 圆角和语义, 默认值为 soft, rounded, neutral, 菜单选项保留选中仅对勾, 正文链接和过程展开项保留内容布局
- `Panel` 统一容器, `SessionSwitcher` 统一会话菜单, `ReviewPanel` 负责评审编辑和撤销, `AnnotationPopover` 负责备注标签和浮窗, 每项提供独立规范页
- 行内工作台复用消息, `WorkedSummary`, 工具, 子代理, `Composer` 和 `ContextEditor`, 页面只处理会话数据, 组件间事件与布局, 不复制菜单, 编辑器或浮窗行为
- 评审清单使用紧凑行, 编辑与评论常驻行末, 编辑替换原文字位置, 分隔线用于分组
- 备注以小标签呈现, hover, 键盘聚焦或点击展示详情, 编辑与删除分别提供入口, 浮窗在外壳内保留 4px 留白, 引用与备注共用圆角内容区, 查看, 编辑与 hover 时始终与面板同色, 浅色主题为白底, 深色主题读取对应面板底色, 只有具体按钮在 hover 时使用 `--action-hover-bg` 灰底, 内部不使用分割线, 标签与 12px 间距区分内容
- 输入框中合并显示已确认和待处理数量, 与备注数量标签共同使用两侧平直的 10px 圆角矩形, 作为状态摘要保留常显浅灰背景, 不随可选操作的透明背景规则隐藏, 不单独占用标题栏
- 上下文分组只保留工作草稿与待处理标题, 不重复展示分组副标题, 底栏只保留右对齐确认按钮, 未确认状态由输入框计数入口提示, 操作反馈显示在底栏之前
- 上下文条目上下内边距 6px, 操作保持同一行, 仅组件实际容器宽度不超过 360px 时将操作放到下一行, 两行之间保留 2px
- 待处理内容不随消息发送, 明确并入后进入编辑草稿, 确认后才替换已确认快照
- 草稿允许纳入, 排除, 排序, 移除, 添加和并入, 未确认时发送继续使用上次确认内容
- 用户发送时保存上下文快照, 评审引用保留当时的文本与备注, 后续编辑不修改历史消息
- 编辑已核对条目后清除该条核对状态, 取消保留旧文本, 支持撤销
- 无未保存备注时正常发送, 存在未保存备注时返回备注编辑器, 保留输入草稿
- 受控输入由页面传入 `value/onValueChange`, 用 `onBeforeSend` 检查页面状态, 同步返回 `false` 时保留草稿, 输入组件不访问评审或备注的内部状态
- `Composer` 的 `accessory` 插槽接收共享备注组件, 非活动会话传入 `active={false}` 关闭菜单与浮窗, 会话草稿仍由页面保存
- `useContextEditor` 通过 `initialOpen` 设置初始展开状态, 通过 `triggerRef` 指定收起时的焦点返回位置, 多会话页面不依赖全局查找首个触发按钮

## 布局与可访问性

- 规范列表的章节标题与规范条目共用标题列和说明列, 标题列按本节最长标题的实际宽度排列, 与说明间隔 16px, 不使用按页面比例扩张的标题列, 760px 以下改为上下排列, 卡片组与后接的规范列表之间保留 24px 间距

- 输入框与对话栏最大 680px, 在可用区域内居中, 关闭评审面板后回到页面中央, ReviewPanel 最大 560px, 宽屏不按屏幕一半持续拉伸, 窄屏叠放并使用可用宽度
- 添加菜单最大 320px, 以触发按钮左侧为锚点向右展开, 窄容器内保留两侧 8px 留白, 不随输入框铺满
- 代码, 路径与长标题允许换行, 不出现水平溢出或按钮重叠
- 使用原生按钮与输入控件, 图标操作有对象明确的可访问名称, 状态变化包含文字
- 菜单支持方向键, Enter, Escape, 点击外部关闭与焦点返回, 输入期间不把 IME 确认键当作发送
- hover 不改变几何尺寸, 动效读取共享配置, 减少动态效果时直接切换状态
- 深浅主题具有相同功能和层级, 色彩由语义变量切换, 不在示例中写死主题值

## 修改与验收

依次修改共享配置, 组件实现和页面组合, 同步核对 [基础规范](system/foundations.html#unified-standards), [控件几何](system/foundations.html#control-geometry), [行内工作台](system/examples/workbench.html) 与 [连续对话](system/app.html)

检查默认, hover, 选中, 聚焦和禁用状态, 验证编辑, 备注, 菜单, 上下文确认及发送快照, 覆盖桌面和窄屏, 浅色和深色, 分别模拟精细指针与粗指针, 不把窄窗口当作触屏

页面中的组件应能追溯到 `system/components` 的实现, 新交互先进入共享组件并提供独立规范页, 再由集成示例引用, 公开接口和加载顺序见 [README](README.md)

运行 `system/verification/geometry.mjs` 回归控件尺寸, hover 不变形, 状态标签, 上下文密度, 类别色和 Agent 身份稳定性, 浏览器模拟结果不代替真实设备验收

历史调研的取舍见 [设计依据](system/design-rationale.md), 过期样式不覆盖当前配置与用户确认的规则
