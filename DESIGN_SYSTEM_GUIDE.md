# 设计系统使用指南

供 Agent 和维护者在新增或修改 context-editor-ui 的组件, 页面和设计规范时使用, 设计方法与实现共同维护在本仓库, 从本文件开始, 按任务读取相关规范和源码

## 入口与事实来源

| 需要了解的内容 | 读取位置 |
| --- | --- |
| 如何分析任务, 选择组件, 验证和记录决定 | 本指南 |
| 已确认的视觉原则, 交互和内容边界 | [DESIGN.md](DESIGN.md) |
| 设计变量, 五级规范, 状态规则和组件参数 | [共享配置](system/design-system.config.js) |
| 组件目录, 公开接口和加载顺序 | [README.md](README.md) |
| 组件结构与行为 | [共享组件目录](system/components), 对应组件的源码与 spec.html |
| 视觉, 主题与响应式状态 | [共享样式](system/styles.css) |
| 实际页面组合 | [行内工作台](system/examples/workbench.html), [连续对话](system/app.html) |
| 已完成的验证与检查入口 | [验证记录](system/verification/README.md) |

`system` 是唯一实现目录, 在当前仓库修改, 不新建源码副本, 不依赖全局技能或当前会话才能理解设计要求, 历史会话用于查找决定的背景, 已确认结论应写回对应规范

本指南负责使用方法, 不复制完整变量表或组件 API, 具体数值读取共享配置, 行为与适用边界读取 DESIGN.md 和组件规范, 发现规范与实现冲突时说明冲突并在本次相关范围内修正

[iOS 阅读与备注小样](system/examples/studies/ios-reading/index.html) 是候选方案, 尚未选定为正式规范, 其独立样式只用于设计验证, 不作为正式页面复制组件样式的先例, 采用候选方案时应把确认的变化纳入共享配置, 组件和规范

## 按任务决定工作范围

用户已明确的颜色, 文案, 尺寸或间距调整, 直接定位共享来源并验证受影响页面, 不强制重新研究或提供多个方案

浅色页面背景优先白色, 需要区分表面时可使用接近白色的淡灰, 单独观看应呈现白色观感, 避免依靠大面积可辨识灰底建立层次, 优先调整间距、圆角、细边界与阴影, 这是 2026-09-23 确认的背景偏好, 当前手机小样使用 #FFFFFF 画布与 #FCFCFD 说明区分组底色

新增组件或改变交互时, 先检查实际界面和调用位置, 用几句话确定用户要完成什么, 必须可见的信息与操作, 操作后果, 数据由谁保存以及可复用的组件, 缺少证据的判断标为假设

Agent 根据任务自主选择已有原则, 变量, 组件和页面模板, 不需要逐项让用户批准, 只有缺失信息会改变任务方向时才提问, 用户要求先看方案再选择时, 先交付可查看的方案

纯后端或不影响界面的修改不执行视觉设计流程, 局部调整不自动扩展成全站重设计

## 从任务选择组件与结构

优先使用现有组件, 再考虑组合现有组件, 只有具体需求无法表达时才扩展变体或新增组件, 说明现有能力缺少什么, 不因参考产品使用了某种外观就新增类型

| 用户任务 | 优先复用与边界 |
| --- | --- |
| 阅读消息, 查看过程或工具输出 | MessageBubble, ToolCall, SubagentCall, 保留内容与过程的层级 |
| 输入消息并选择模型, 权限和上下文 | Composer, 通过受控输入与 accessory 插槽组合附属内容 |
| 整理下一条消息的上下文 | ContextEditor, 保持待处理, 工作草稿与已确认快照的区别 |
| 持续核对和修改一组内容 | ReviewPanel, 复用核对, 行内编辑和撤销 |
| 查看或编辑一处引用的备注 | AnnotationPopover, 不为短备注复制一个评审面板 |
| 切换会话或组合面板 | SessionSwitcher 与 Panel, 页面保存会话数据, 容器不承担业务状态 |
| 执行独立文案动作 | Button, 分别选择 variant, shape 和 tone, 菜单选项与正文链接按自身语义呈现 |
| 展示资料标签或头像集合 | ProfileTags 与 AvatarGroup, 不给静态内容增加隐含操作 |

先确定信息关系, 阅读顺序与操作位置, 再选择密度, 色彩和形状, 具体规则见 DESIGN.md, 对话与评审页面从已有模板组合, 不默认套用无关的仪表盘结构

## 参考与方案比较

参考产品用于发现遗漏和检验假设, 分开说明观察到的形式, 推测的原因以及本项目适用条件, 不把外观模仿或作者解释当作用户研究证据

存在影响使用的取舍时, 用局部方案比较一个决定, 如浮窗与侧栏, 常驻与按需操作, 保持内容和无关变量一致, 说明各方案的作用和代价并给出建议, 已确认的决定不反复重开比较

已有规范能解决的选择直接实施, 不为凑数量制造方案, 可逆且已授权的调整不增加逐步审批

## 在共享来源实现

- 变量, 参数与规范文字维护在共享配置, 视觉与响应式状态维护在共享样式, 不在页面写死一份组件样式或行为
- 新交互先进入 system/components 的共享组件并提供独立规范页, 页面负责数据, 布局与事件连接, 按 README 的接口与加载顺序集成
- Composer 使用受控输入与 accessory 插槽, onBeforeSend 同步返回 false 时保留草稿, 不让输入组件访问评审或备注的内部状态
- 待处理内容先并入草稿, 确认后才替换发送快照, 未确认修改不进入消息, 后续修改不改变历史消息保存的上下文和评审引用
- 多会话通过 active 和 triggerRef 隔离菜单与焦点, 会话草稿由页面保存, Agent 身份在开始和完成状态保持一致
- 颜色, 字体, 圆角, 图标, 动效和点击区复用已有语义变量, 只有观察到具体缺口时才新增参数, 局部改动保留其他已确认约束

## 按影响范围验证

在真实内容与显示尺寸下检查信息分组, 对齐, 组内和组间间距, 文字与图标的视觉重量, 操作结果是否明确, 不用多余分割线代替层级, 不把比例公式当作视觉质量的证明

界面变更检查正常内容和至少一个相关压力情况, 如长文案, 空内容, 多条内容或窄窗口, 覆盖本次涉及的默认, hover, focus, 选中, 编辑, 禁用或错误状态, 不机械要求每个控件具有所有状态

修改共享组件时, 同时检查其独立规范页和实际调用页面, 用截图检查视觉, 用实际操作检查行为, 涉及菜单时检查键盘导航, Escape, 外部关闭和焦点返回, 涉及发送时检查草稿保留, IME 和确认快照

涉及共享视觉或响应式变量时, 核对浅色和深色, 桌面和窄屏, 根据点击区变化分别模拟精细指针和粗指针, 窄窗口不等于触屏, 浏览器模拟不等于真实设备验收

按回归风险选择 [已有验证脚本](system/verification/README.md), 不为只改文档运行全套交互测试, 不用代码审查或截图代替操作验证, 无法运行时说明原因与未验证部分

## 记录与维护

新增组件, 新增变体或改变规则时, 在已有组件规范或对应设计章节记录决定, 解决的问题与证据, 适用边界和验证结果, 有现成验证记录时直接引用, 已有规范能解释的局部调整不重复写决策

组件特例保留在组件规范, 经过相关场景验证并说明边界后才提升为全局规则, 规则变化同步修正冲突的配置, 文档和示例, 不另建一套平行规范

交付说明改动及原因, 已完成的检查与未验证部分, 使用中文, 保留系统名称, 代码标识和模型原名, 用具体影响说明结果
